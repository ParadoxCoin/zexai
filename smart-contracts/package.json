{
  "name": "smart-contracts",
  "version": "1.0.0",
  "description": "ZexAI Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:amoy": "hardhat run scripts/deploy.js --network polygonAmoy",
    "deploy:base": "hardhat run scripts/deploy.js --network baseSepolia",
    "test": "hardhat test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "dotenv": "^16.4.0"
  }
}